---
"@trinacria/cli": minor
---

Add `trinacria new` scaffolding command using `apps/*` templates (excluding playground), with package.json adaptation for standalone projects and new CLI/docs/tests coverage.
